# Roof Runner

A single-file browser game built with [Phaser](https://phaser.io/) (loaded from CDN — no build step).

## Play

https://jcleary.github.io/roof-runner/

## Run locally

Just open `index.html` in a browser, or serve the folder:

```
python3 -m http.server
```
